// Exercise 15 - Clear The Clutter
// Objective:
    // Write a Node.js program that organizes the files inside a given directory by sorting them into separate folders based on their file extensions.

// Details:
    // Read all files inside a specified directory.
    // Identify the file extensions (e.g., .png, .jpg, .pdf).
    // Create separate folders for each file extension (e.g., PNG Files, JPG Files, PDF Files).
    // Move files into their respective extension-based folders.